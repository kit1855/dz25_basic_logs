all:
  children:
    web_group:
      hosts:
        web:
          ansible_host: web
          ansible_user: vagrant
    log_group:
      hosts:
        log:
          ansible_host: log
          ansible_user: vagrant
